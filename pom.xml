<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <groupId>com.github.bisq-network.tor-binary</groupId>
    <artifactId>tor-binary</artifactId>
    <version>0.4.9.11</version>

    <modelVersion>4.0.0</modelVersion>
    <packaging>pom</packaging>

    <properties>
        <!-- change here to get the desired version, verify signature, unpack and package tor binary

        requirements:
        - gpg in path for PGP signature verification
        - tar with gzip/xz support in path
        -->
        <torbrowser.version>15.0.21</torbrowser.version>
        <torbrowser.download.base.url>https://archive.torproject.org/tor-package-archive/torbrowser/${torbrowser.version}</torbrowser.download.base.url>
        <torbrowser.checksum.source.url>${torbrowser.download.base.url}/sha256sums-signed-build.txt</torbrowser.checksum.source.url>
        <pgp.signature.keyserver>hkps://keys.openpgp.org</pgp.signature.keyserver>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.remote-resource.directory>${project.build.directory}/maven-shared-archive-resources</project.remote-resource.directory>
    </properties>

    <licenses>
        <license>
            <name>GNU AFFERO GENERAL PUBLIC LICENSE</name>
            <url>http://www.gnu.org/licenses/agpl-3.0.html</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/bisq-network/tor-binary/issues</url>
    </issueManagement>

    <developers>
        <developer>
            <name>Cédric Walter</name>
            <email>cedric.walter@gmail.com</email>
            <organization>None</organization>
            <organizationUrl>www.cedricwalter.com</organizationUrl>
        </developer>
        <developer>
            <name>JesusMcCloud</name>
        </developer>
        <developer>
            <name>Florian Reimair</name>
            <email>office@florianreimair.at</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/bisq-network/tor-binary</connection>
        <developerConnection>scm:git:https://github.com/bisq-network/tor-binary</developerConnection>
        <url>scm:git:https://github.com/bisq-network/tor-binary</url>
    </scm>

    <modules>
        <module>tor-binary-resources</module>
        <module>tor-binary-linux32</module>
        <module>tor-binary-linux64</module>
        <module>tor-binary-macos</module>
        <module>tor-binary-macos-aarch64</module>
        <module>tor-binary-windows</module>
        <module>tor-binary-windows64</module>
        <module>tor-binary-geoip</module>
    </modules>

    <profiles>
        <profile>
            <id>check-pgp-signatures</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>1.8</version>
                        <inherited>false</inherited>
                        <executions>
                            <execution>
                                <id>check-pgp-signatures</id>
                                <phase>verify</phase>
                                <configuration>
                                    <target>
                                        <property name="pgp.signature.check.directory"
                                                  value="${project.build.directory}/pgp-signature-check"/>

                                        <macrodef name="fetch-tor-bundle">
                                            <attribute name="bundle"/>
                                            <sequential>
                                                <get
                                                    src="${torbrowser.download.base.url}/@{bundle}"
                                                    dest="${pgp.signature.check.directory}/@{bundle}"
                                                    skipexisting="true"/>
                                                <get
                                                    src="${torbrowser.download.base.url}/@{bundle}.asc"
                                                    dest="${pgp.signature.check.directory}/@{bundle}.asc"
                                                    skipexisting="true"/>
                                            </sequential>
                                        </macrodef>

                                        <property name="torbrowser.checksum.file"
                                                  value="${project.basedir}/tor-binary-resources/checksums/sha256sums-signed-build.txt"/>

                                        <delete dir="${pgp.signature.check.directory}" failonerror="false"/>
                                        <mkdir dir="${pgp.signature.check.directory}"/>

                                        <!-- Checked before the bundles are downloaded. This call also creates the
                                             verification keyring that the second call reuses. -->
                                        <ant antfile="${project.basedir}/verification.xml" target="verify-checksum-manifest" inheritall="false">
                                            <property name="checksum.file" value="${torbrowser.checksum.file}"/>
                                            <property name="checksum.source.url" value="${torbrowser.checksum.source.url}"/>
                                            <property name="pgp.signature.keyserver" value="${pgp.signature.keyserver}"/>
                                            <property name="pgp.keyring.directory" value="${pgp.signature.check.directory}/verification-keyring"/>
                                        </ant>

                                        <parallel>
                                            <fetch-tor-bundle bundle="tor-expert-bundle-windows-i686-${torbrowser.version}.tar.gz"/>
                                            <fetch-tor-bundle bundle="tor-expert-bundle-windows-x86_64-${torbrowser.version}.tar.gz"/>
                                            <fetch-tor-bundle bundle="tor-expert-bundle-macos-x86_64-${torbrowser.version}.tar.gz"/>
                                            <fetch-tor-bundle bundle="tor-expert-bundle-macos-aarch64-${torbrowser.version}.tar.gz"/>
                                            <fetch-tor-bundle bundle="tor-expert-bundle-linux-i686-${torbrowser.version}.tar.gz"/>
                                            <fetch-tor-bundle bundle="tor-expert-bundle-linux-x86_64-${torbrowser.version}.tar.gz"/>
                                        </parallel>

                                        <ant antfile="${project.basedir}/verification.xml" target="verify-bundle-signatures" inheritall="false">
                                            <property name="bundle.directory" value="${pgp.signature.check.directory}"/>
                                            <property name="torbrowser.version" value="${torbrowser.version}"/>
                                            <property name="pgp.signature.keyserver" value="${pgp.signature.keyserver}"/>
                                            <property name="pgp.keyring.directory" value="${pgp.signature.check.directory}/verification-keyring"/>
                                            <property name="pgp.keyring.prepared" value="true"/>
                                        </ant>

                                        <delete dir="${pgp.signature.check.directory}" failonerror="false"/>
                                    </target>
                                </configuration>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
